import React from 'react';

const Preview = () => {
    return (
        <div>
            <h2 className="text-xl font-bold mb-4">Preview</h2>
            {/* Render all the collected data here */}
        </div>
    );
};

export default Preview;
